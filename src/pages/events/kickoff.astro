---
title: FRC Kickoff | FIRST® Alumni & Mentors Network at Michigan
sponsors:
    - 0:
        link: "https://esg.engin.umich.edu"
        image: "esg.png"
        name: "Engineering Student Government"
    - 0:
        link: "https://engin.umich.edu"
        image: "coe.png"
        name: "University of Michigan College of Engineering"
schedule:
    times:
        - 09:00 AM
        - 09:30 AM
        - 10:00 AM
        - 10:30 AM
        - 11:00 AM
        - 11:30 AM
        - 12:00 PM
        - 12:30 PM
        - 01:00 PM
        - 01:30 PM
        - 02:00 PM
        - 02:30 PM
        - 03:00 PM
        - 03:30 PM
        - 04:00 PM
        - 04:30 PM
        - 05:00 PM
carousel_items:
    - title: Engineering Organization Fair
      caption: Teams learn about Michigan Engineering's various student organizations along with the new Undergraduate Robotics program
      image: org-fair-ground.jpeg
      webp: org-fair-ground.webp
    - title: Michigan Robotics Presentation
      caption: Damen Provost, the Managing Director of the University of Michigan Robotics Institute, introduces the institute's mission and opportunities to teams
      image: rob-presentation.jpeg
      webp: rob-presentation.webp
    - title: The FAMNM Event Crew
      caption: From parking assistance to breakout room supervision, FAMNM members and affiliated volunteers ensure a smooth kickoff experience
      image: event-crew.jpeg
      webp: event-crew.webp
    - title: Kit Distribution On Site
      caption: FAMNM distributes the new Kit of Parts for <i>CHARGED UP</i> to attending teams
      image: kit-distribution.jpeg
      webp: kit-distribution.webp
---
<!DOCTYPE html>
<html lang="en">
<head>
    {% include title-styles.html %}
    <style>

        /** Sponsor display list **/
        
        ul.sponsor-display {
            padding: 0;
            vertical-align: middle;
            height: auto;
            margin: 0 auto;
            text-align: center;
        }
        
        @media (max-width: 768px) {
            ul.sponsor-display>li {
                list-style-type: none;
                vertical-align: middle;
            }
            
            ul.sponsor-display>li img {
                max-width: 100%;
            }
            
            #quick-links>.btn {
                margin: 5px auto;
            }
            
            #map {
                height: 400px;
            }
        }
        
        @media (min-width: 768px) {
            ul.sponsor-display>li {
                list-style-type: none;
                display: inline-block;
                vertical-align: middle;
            }
            
            ul.sponsor-display>li img {
                max-width: 250px;
            }
            
            #map {
                height: 600px;
            }
        }
        
        ul.sponsor-display>li:hover {
            background-color: #cccccc;
        }
        
        ul.sponsor-display>li>a {
            display: block;
            padding: 10px;
        }
        
        ul.sponsor-display>li img {
            max-height: 180px;
        }
    </style>
</head>
<body>
    {% include header.html %}
    
    <main>
        <div class="container content">
            <div class="row justify-content-center pb-4">
                <div class="col col-8 col-md-6 col-lg-4 mx-auto p-4 p-md-5">
                    <a href="https://www.firstinspires.org/robotics/frc/game-and-season" aria-label="CHARGED UP season information" rel="noopener noreferrer" target="_blank">
                        <img class="center w-100" src="/img/kickoff/2023/season_logo.png"
                            alt="Charged Up, presented by Haas">
                    </a>
                </div>
                <div class="col col-12 mt-1 mt-md-2">
                    <h1 class="display-6 fw-bold">FAMNM <i>FIRST<sup>&reg;</sup></i> Robotics Competition Kickoff</h1>
                    <p>
                        FAMNM held a kickoff event in person for FRC's 2023 season game,
                        <i>CHARGED UP</i>, on <time datetime="2023-01-07">Saturday, January 7th, 2023</time>
                        at the University of Michigan's <a href="https://goo.gl/maps/SviBGJZca11mxoX19" rel="noopener noreferrer">North Campus</a>.
                        The kickoff featured an engineering student organization fair, an info session about the University of Michigan's new Undergraduate Robotics program,
                        guest speakers, and team breakout rooms, with more than 40 teams in attendance. Good luck, teams!
                    </p>
                </div>
            </div>
        </div>
        <div id="image-carousel" class="carousel slide" data-bs-ride="false">
            <div class="carousel-indicators">
                <button type="button" data-bs-target="#image-carousel"
                    data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
                {% assign replicate_indicator_count = page.carousel_items.size | minus: 1 %}
                {% for i in (1..replicate_indicator_count) %}
                <button type="button" data-bs-target="#image-carousel"
                    data-bs-slide-to="{{ i }}" aria-label="Slide {{ i | plus: 1 }}"></button>
                {% endfor %}
            </div>
            <div class="carousel-inner">
                {% for item in page.carousel_items %}
                    {% if forloop.first %}
                        <div class="carousel-item active">
                    {% else %}
                        <div class="carousel-item">
                    {% endif %}
                        <picture class="carousel-back-image" aria-hidden="true">
                            <source srcset="/img/kickoff/2023/{{ item.webp }}" type="image/webp">
                            <source srcset="/img/kickoff/2023/{{ item.image }}" type="image/jpeg">
                            <img src="/img/kickoff/2023/{{ item.image }}" alt="">
                        </picture>
                        <picture>
                            <source srcset="/img/kickoff/2023/{{ item.webp }}" type="image/webp">
                            <source srcset="/img/kickoff/2023/{{ item.image }}" type="image/jpeg">
                            <img src="/img/kickoff/2023/{{ item.image }}" alt="{{ item.caption }}">
                        </picture>
                        <div class="carousel-caption d-none d-md-block">
                            <h3>{{ item.title }}</h3>
                            <p>{{ item.caption }}</p>
                        </div>
                    </div>
                {% endfor %}
                <button class="carousel-control-prev" type="button" data-bs-target="#image-carousel" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#image-carousel" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div>
        </div>
        <div class="container content">
            <div class="row pt-2">
                <h3>Sponsors</h3>
                <ul class="sponsor-display">
                    {% for sponsor in page.sponsors %}
                        <li><a href="{{ sponsor[0].link }}" target="_blank" rel="noopener noreferrer"><img class="center" src="/img/sponsor/{{ sponsor[0].image }}" alt="{{ sponsor[0].name }}" /></a></li>
                    {% endfor %}
                </ul>
                <ul class="list-unstyled">
                    {% for sponsor in page.sponsors %}
                        <li><a href="{{ sponsor[0].link }}" target="_blank" rel="noopener noreferrer">{{ sponsor[0].name }}</a></li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </main>
        
    
    {% include footer.html %}
    {% include scripts.html %}
    <style>
        .schedule-time {
            text-align: right;
            line-height: 100%;
            width: 5.5rem;
            height: fit-content;
            display: flex;
            align-items: center;
            justify-content: center;
            transform: translateY(-15px);
            padding: 4px;
            border-radius: 6px;
            border: 1px solid #888888;
            background-color: #dfdfdf;
            font-weight: bold;
            grid-column: 1;
        }

        .schedule-time-border {
            border-top: 1px solid #888888;
            margin-top: -2px;
            grid-column: 1/-1;
        }

        #schedule-table td {
            padding: 2px;
            vertical-align: middle;
            margin: 2px;
            font-size: 1.1rem;
        }

        #schedule-table {
            max-width: 42rem;
            display: grid;
            gap: 4px;
            grid-template-columns: 5.8rem repeat(3, 8px) 1fr repeat(3, 4px) repeat(3, 8px) 1fr repeat(3, 4px);
            grid-template-rows: repeat({{ page.schedule.times | size }}, 48px);
        }

        #schedule-table .event {
            font-size: 0.9rem;
            padding: 4px;
            background-color: var(--event-background);
            border-left: 6px solid var(--event-color);
            border-top: 1px solid var(--event-color);
            border-right: 1px solid var(--event-color);
            border-bottom: 1px solid var(--event-color);
            border-radius: 0.25rem;
            text-align: left;
            line-height: 115%;
            vertical-align: top;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        .event-indent-0 {
            --event-color: #003368;
            --event-background: #a3bdda;
        }

        .event-indent-1 {
            --event-color: #005269;
            --event-background: #9ec7d3;
        }

        .event-indent-2 {
            --event-color: #006966;
            --event-background: #a2dad8;
        }

        [v-cloak] {
            display: none;
        }

        .text-location {
            color: #545454;
        }

        .text-location:not([disabled="true"]):after {
            content: "↗";
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }

        .text-location[disabled="true"] {
            pointer-events: none;
            cursor: not-allowed;
        }
    </style>
</body>
</html>
